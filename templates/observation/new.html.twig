{% extends 'base.html.twig' %}

{% block body %}

	<h1>Nieuwe observatie</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}

	<p class="text-muted">
		Bij een foutmelding moet de foto opnieuw gekozen worden.
	</p>
	<button type="submit">Opslaan</button>
	{{ form_end(form) }}

{% endblock %}
