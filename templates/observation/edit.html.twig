{% extends "base.html.twig" %}

{% block title %}Observation bewerken{% endblock %}

{% block body %}
    <h1>Observation bewerken</h1>
    {{ form_start(form)}}
    {{ form_widget(form)}}
    <button class="btn btn-primary">Opslaan</button>
    {{ form_end(form)}}
    <a href="{{ path('app_observation_index') }}">terug naar lijst</a>
{% endblock %}